# Test environment. Override DB and LLM key with env vars; do not commit real keys.
spring.datasource.url=${DB_URL:jdbc:postgresql://192.9.51.198:5432/guass_test}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:zwk-nur-jaz3NXZ@ktv}
spring.jpa.hibernate.ddl-auto=update

spring.ai.openai.api-key=${OPENAI_API_KEY:}
spring.ai.openai.base-url=${OPENAI_BASE_URL:https://api.openai.com}

logging.level.root=INFO
logging.level.org.springframework.ai=INFO
logging.level.com.example.agentplatform=INFO
